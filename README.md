# 🎤 OPENwebKTV (OwK)

**基于 Bilibili 曲库的局域网家庭 KTV 系统** —— 电视大屏播放 + 手机网页遥控，一台普通电脑即可部署，免费、无会员、无广告。

> 电视打开一个网页就是点唱机，手机扫码就是遥控器；搜得到的 B 站视频，都是你的曲库。

---

## ✨ 相比其他 KTV 软件的优势

| 对比项 | 传统点歌机 / 商用 KTV 软件 | **OPENwebKTV** |
|---|---|---|
| 费用 | 曲库年费、设备费、会员 | **完全免费**，无任何订阅 |
| 曲库 | 固定且更新慢，冷门歌没有 | **B 站海量曲库**：热门新歌、冷门、外语、ACG、MV、演唱会现场都能搜到 |
| 客户端 | 需安装专用 App / 点歌机 | **纯网页**：电视用 Chrome/Edge/任意浏览器打开即可，无需安装 |
| 遥控 | 专用遥控器/App 配对 | **手机浏览器扫码即连**，无配对、无蓝牙、无安装 |
| 数据 | 走云端，可能被限流/断网失联 | **局域网直连**：无云端中转，已下载歌曲断外网也能播 |
| 设备兼容 | 绑定特定硬件 | 任意电视/投影/电脑/电视盒子，可选 Android TV 壳；**安卓机顶盒/电视/手机本身也可用 Termux 跑后端** |
| 部署门槛 | 需购买点歌机/专用硬件 | 电脑、NAS、**甚至安卓机顶盒/电视/手机（Termux）**都能跑，方法与电脑一致 |
| 歌词 | 单行/卡拉OK式 | **两行大屏滚动歌词**，焦点放大+黑边阴影；多来源（B站字幕/第三方 LRCLIB） |
| 播放控制 | 基础 | 队列管理、切歌/上一首、进度拖拽、音量、空闲自动待机 |
| 下载 | 无 | 点歌自动后台下载（DASH 音视频分离 → H.264 合并），可离线反复唱 |
| 安全 | 无 | 「高级操作」静态密码 + 电视动态码双因子，访客无法误删歌曲/退出登录 |

**一句话**：别人家是"买了点歌机才有歌可唱"，OwK 是"B 站有什么就能唱什么"，还不用装任何 App。

---

## 📦 功能一览

- **搜索点歌**：B 站搜索自动拼"歌曲"，标题含音质/MV/书名号的优质结果自动置顶
- **下载预览**：点 ⬇ 进入预览页（B 站官方播放器不自动播放 + 视频详情 + 歌词），可**选择歌词来源**（B 站多字幕轨 / 第三方歌词 / 无歌词），下载带进度条
- **播放队列**：点歌即入队自动下载自动播放；队列支持置顶 / 上移 / 下移 / 删除 / 上一首回溯
- **两行滚动歌词**：大屏清晰可读，焦点句放大，支持多来源歌词
- **B 站登录**：播放端（电视）显示二维码 → 手机扫码 → 自动保存登录态；未登录时控制端全屏锁定，仅可刷新二维码
- **高级操作**（双因子验证）：退出登录 B 站 / 删除本地歌曲（含文件）/ 清空播放列表，防止访客误操作
- **静音自适应**：浏览器自动播放被拦截时静音开播并提示「按 回车/OK/空格 开启声音」
- **Android TV 壳**（可选）：`player-android/` 提供 Kotlin WebView 壳，支持局域网扫描连接

---

## 🚀 部署方式

### 1. 环境要求

- **Python 3.10+**（开发于 3.13）
- **ffmpeg**（系统命令，下载视频合成必需）
  - Ubuntu/Debian：`sudo apt install ffmpeg`
  - Windows：下载 [ffmpeg](https://ffmpeg.org/download.html) 并加入 PATH
- 无需数据库服务器，SQLite 内嵌单文件存储

### 2. 安装依赖

```bash
cd OPENwebKTV
pip install -r backend/requirements.txt
```

> 建议使用虚拟环境：`python3 -m venv .venv && source .venv/bin/activate`（可选）。

### 3. 启动

```bash
python3 start.py
```

启动后控制台会打印三个地址（端口默认 **8080**，被占用时自动列出占用程序）：

```
内网:   http://192.168.x.x:8080
播放器: http://192.168.x.x:8080/player.html     ← 电视/大屏打开这个
控制器: http://192.168.x.x:8080/controller.html ← 手机打开这个
```

### 4. 首次使用（B 站登录）

1. 电视打开 `player.html`，未登录时全屏显示 **B 站登录二维码**
2. 手机扫码并在 B 站 App 确认
3. 登录态自动保存到 `backend/bilibili_cookie.json`，播放端自动刷新恢复正常服务
   - 之后搜索/下载/歌词均可用；退出登录需通过「高级操作」

### 5.（可选）设置高级操作静态密码

默认静态密码 `1234`，两种改法：

```bash
# 方式一：环境变量（推荐）
OWK_ADMIN_PASSWORD=你的密码 python3 start.py

# 方式二：改 backend/app/config.py 中的 ADMIN_STATIC_PASSWORD
```

### 6.（可选）Android TV 壳

`player-android/` 为 Kotlin 工程，需 Android SDK（Gradle 构建），构建后安装到电视即可全屏加载 player.html，支持同网段自动扫描服务器。

### 7.（可选）安卓机顶盒 / 电视 / 手机：用 Termux 部署

后端是纯 Python + ffmpeg，**任何能装 Termux 的安卓设备**都可以当服务器，方法跟电脑一模一样：

```bash
# 1. 安装 Termux（建议用 F-Droid 版本，勿用 Play 商店旧版），然后：
pkg update && pkg upgrade
pkg install python ffmpeg git

# 2. 获取项目
git clone https://github.com/wzmwayne/OPENwebKTV.git
cd OPENwebKTV

# 3. 安装依赖
pip install -r backend/requirements.txt

# 4. 启动（与电脑完全相同）
python start.py
```

注意事项：

- **保持后台运行**：Android 12+ 对后台进程限制严格，Termux 请保持在前台，或安装 `termux-wake-lock` 保活：
  ```bash
  pkg install termux-api && termux-wake-lock
  ```
- **同机自用**：手机/机顶盒自己当服务器时，直接浏览器打开 `http://127.0.0.1:8080/player.html`（电视大屏）和 `http://127.0.0.1:8080/controller.html`（手机遥控）；其他设备用启动时打印的 `http://<本机IP>:8080` 访问
- **媒体目录**：默认存在项目 `backend/data/media/`，无需额外存储权限；想下载到共享存储可 `termux-setup-storage` 后自行调整路径
- **性能**：机顶盒/手机跑下载合并（ffmpeg）会比电脑慢，但播放完全无压力；电视盒注意散热

---

## 🗂 目录结构

```
OPENwebKTV/
├── start.py                 # 启动入口（自动进入 backend 执行）
├── backend/
│   ├── requirements.txt     # Python 依赖
│   ├── run.py               # 端口探测 + 启动 uvicorn
│   ├── app/                 # FastAPI 后端（路由/播放引擎/WS/B站客户端）
│   ├── data/                # 运行时生成：SQLite 数据库 + media/ 下载目录
│   └── bilibili_cookie.json # B 站登录凭证（自动生成，勿提交）
├── frontend/
│   ├── player.html          # 播放器页（电视）
│   └── controller.html      # 控制器页（手机）
└── player-android/          # 可选 Android TV 壳
```

---

## ❓ 常见问题

- **端口被占用**：启动时自动列出占用程序与 PID，排查后重启即可
- **播放黑屏只有声音**：下载管线已优先 H.264 并自动转码，兼容主流浏览器
- **没有声音**：浏览器自动播放策略可能拦截了有声播放，按 **回车 / OK / 空格** 开启声音（电视遥控器 OK 键也可）
- **前几秒有声音后自动静音**：已修复（代际隔离 + 实时静音监测），若复现请反馈
- **歌词内容对不上**：B 站 AI 字幕偶有错乱，已用 WBI 签名接口修复并优先非 AI 轨；可在下载时选择第三方歌词
- **未登录时点歌失败**：请先在播放端扫码登录 B 站
- **队列上限**：最多 50 首，同一首歌不能重复点

---

## ⚖️ 免责声明

本项目仅供**个人学习与家庭娱乐**使用。下载的视频/歌词版权归原作者及版权方所有，请勿用于商业用途或公开分发。B 站接口为公开接口，请遵守 B 站服务条款，避免高频抓取。使用本项目产生的一切后果由使用者自行承担。
