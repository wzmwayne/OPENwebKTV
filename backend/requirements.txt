# OPENwebKTV 后端依赖
# 环境: Python 3.10+ (开发于 3.13); 系统还需安装 ffmpeg(命令行, 下载合成必需)
# 安装: pip install -r requirements.txt

fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.27.0
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
pydantic>=2.0.0
python-multipart>=0.0.9
qrcode>=7.4          # B站扫码登录二维码生成
pillow>=10.0.0       # qrcode 图像后端(必需)
